<div>
    <input type="text" class="input-field" placeholder="@PlaceHolderText" @bind-value="@inputValue" />
</div>

@code {
    private string inputValue;

    private bool componentLocalBool;
    [Parameter]
    public string StringValue
    {
        get => inputValue;
        set
        {
            if (inputValue == value) return;
            value = inputValue;
            StringValueChanged.InvokeAsync(inputValue);
        }
    }
    [Parameter]
    [EditorRequired]
    public string? PlaceHolderText { get; set; }

    [Parameter]
    public EventCallback<string> StringValueChanged { get; set; }

}
